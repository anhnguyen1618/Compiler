/* a record type and a record variable */
let
	type  rectype = {name:string, age:int}
	var rec1:rectype := rectype {name="Nobody", age=10}
in
	rec1.name := "Somebody";
    rec1.age := rec1.age + 5;
	print(rec1.name);
    print("\n");
    printInt(rec1.age)
end